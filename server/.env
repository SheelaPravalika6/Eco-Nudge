# ── Required ────────────────────────────────────────────────
JWT_SECRET=econudge_super_secret_key_change_this_in_production
PORT=5000

# ── Set these on Railway (not needed locally) ────────────────
# DB_PATH=/app/data/econudge.db        ← path inside Railway persistent volume
# FRONTEND_URL=https://your-app.vercel.app  ← your Vercel frontend URL
